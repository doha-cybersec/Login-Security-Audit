# Login-Security-Audit
This project demonstrates how to store and analyze login attempts using Oracle SQL command line to detect potential security threats, such as brute-force attacks or suspicious IP addresses. It uses only SQL commands
